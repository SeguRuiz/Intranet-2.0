import { Tooltip } from "react-leaflet";
import { Button } from "@mui/material";
import { useSelector } from "react-redux";

const OpenAddComunicaciones = () => {
//   const { edit_com } = useSelector((e) => e.Comunicaciones);
 
  return (
    <>
    
    </>
  );
};

export default OpenAddComunicaciones;
